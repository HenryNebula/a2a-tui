# Build artifacts
/bin/
/dist/

# Test artifacts
*.test
coverage.out

# Local environment / secrets — never commit
.env
.env.*

# Editor / OS noise
.DS_Store

# Local assistant configuration (machine-specific)
.claude/

# Stray root-level build outputs (go build ./cmd/... without -o)
/a2a-tui
/a2a-fixture-agent
scripts/visual/out/
